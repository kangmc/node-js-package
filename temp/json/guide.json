{
    "title" : "Project Guide",
    "desc" : "js 파일 등록 및 전역 설정, 페이지 단위 설정하는 방법을 익힌다.",
    "child" : [
        {
        	"title" : "js 파일 등록", "desc" : "아래의 내용을 <b>&lt;/head&gt;</b> Tag 바로 위에 작성한다.",
        	"code" : "", 
        	"child": [
        	{
        		"title" : "$.config > set", 
        		"desc" : "configuration 정보중 editable:true 인 값들만 runtime 에서 1회 설정이 가능하다.",
	        	"code": "var root = $.config('root');"
	        },
	        {
        		"title" : "$.config > get", 
        		"desc" : "설정된 정보를 get : readonly.",
	        	"code": "var root = $.config('root');"
	        },
        ]},
        {"title" : "$.log", "desc" : "설정된 application configuration 정보를 get/set", "child": [
        	{
        		"title" : "$.config > set", 
        		"desc" : "configuration 정보중 editable:true 인 값들만 runtime 에서 1회 설정이 가능하다.",
	        	"code": "var root = $.config('root');"
	        },
	        {
        		"title" : "$.config > get", 
        		"desc" : "설정된 정보를 get : readonly.",
	        	"code": "var root = $.config('root');"
	        },
        ]}
    ] 
}
